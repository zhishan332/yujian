<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<!-- Created with MTool.  -->
<mapper namespace="com.yujian.wq.mapper.ImgWorkMapper">

    <resultMap id="imgMap" type="ImgEntity">
        <id property="id" column="id" jdbcType="INTEGER"/>
        <result property="img" column="img" jdbcType="VARCHAR"/>
        <result property="title" column="title" jdbcType="VARCHAR"/>
        <result property="tagId" column="tag_id" jdbcType="INTEGER"/>
        <result property="chain" column="chain" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
    </resultMap>


    <insert id="insertImgAndGetId" useGeneratedKeys="true" keyProperty="id" parameterType="ImgEntity">
        insert into img
        (img,tag_id,chain,title,md5)
        values(
        #{img,jdbcType=VARCHAR},
        #{tagId,jdbcType=INTEGER},
        #{chain,jdbcType=VARCHAR},
        #{title,jdbcType=VARCHAR},
        #{md5,jdbcType=VARCHAR}
        )
    </insert>


    <insert id="insertTag" useGeneratedKeys="true" keyProperty="id" parameterType="TagEntity">
        insert into tag
        (img_id,tag)
        values(
        #{imgId,jdbcType=INTEGER},
        #{tag,jdbcType=VARCHAR}
        )
    </insert>


    <select id="find" resultMap="imgMap">
        select
        id,img,tag_id,title,chain
        from img
        <where>
            tag_id = #{tagId,jdbcType=INTEGER}
        </where>
        order by id desc
        limit #{start,jdbcType=INTEGER},#{num,jdbcType=INTEGER}
    </select>

    <insert id="insertChain" useGeneratedKeys="true" keyProperty="id" parameterType="ImgEntity">
       insert into img_chain
        (img,tag_id,chain,title,md5)
        values(
        #{img,jdbcType=VARCHAR},
        #{tagId,jdbcType=INTEGER},
        #{chain,jdbcType=VARCHAR},
        #{title,jdbcType=VARCHAR},
        #{md5,jdbcType=VARCHAR}
        )
    </insert>

    <select id="findChain" resultMap="imgMap">
        select
        id,img,tag_id,title,chain
        from img_chain
        where chain = #{chain,jdbcType=VARCHAR}
        limit 1
    </select>

    <select id="findChainByTag" resultMap="imgMap">
        select
        id,img,tag_id,title,chain
        from img_chain
        where chain = #{chain,jdbcType=VARCHAR} and tag_id =  #{tagId,jdbcType=INTEGER}
        limit 1
    </select>


    <select id="findTotal" resultType="java.lang.Integer">
        select
        count(*)
        from img
        where tag_id !=5
    </select>

    <select id="findRandom" resultMap="imgMap">
        select
        id,img,tag_id,title
        from img
        where id in
        <foreach item="item" index="index" collection="list" open="(" separator="," close=")">
            #{item}
        </foreach>
        and tag_id !=5
        limit #{num,jdbcType=INTEGER}
    </select>


    <select id="findNotIn" resultMap="imgMap">
        select
        id,img,tag_id
        from img
        <where>
            tag_id != #{tagId,jdbcType=INTEGER}
        </where>
        order by id desc
        limit #{start,jdbcType=INTEGER},#{num,jdbcType=INTEGER}
    </select>

</mapper>